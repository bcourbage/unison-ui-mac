<?xml version="1.0" standalone="yes"?>
<!-- sparkle-sign-warning:
IMPORTANT: This file was signed by Sparkle. Any modifications to this file requires re-signing this file with generate_appcast or sign_update! The signed signature will be embedded at the end of this file.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>unison-ui-mac</title>
        <item>
            <title>0.7.0</title>
            <pubDate>Sun, 06 Sep 2026 09:40:21 +0000</pubDate>
            <sparkle:version>23</sparkle:version>
            <sparkle:shortVersionString>0.7.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.7.0</h1>
<p>Version 0.7.0 adds a <code>unison</code> command to the app. One install now serves both the graphical interface and Unison's command-line roles, including acting as the far side of an SSH profile, so a Mac with this app no longer needs a separate Unison installation. Existing profiles and settings continue to work without migration.</p>
<h2>Added</h2>
<ul>
<li><strong>The <code>unison</code> command.</strong> The app bundle carries a command-line launcher. Once linked onto your PATH under the name <code>unison</code>, it behaves like Unison's own command: <code>unison -ui graphic</code> opens this app, <code>unison &lt;profile&gt;</code> and <code>unison -ui text &lt;profile&gt;</code> run Unison's text interface in the terminal on the app's embedded engine, and <code>unison -server</code>, which another machine runs over ssh when it syncs to this Mac, is served by the app's engine headlessly. Arguments reach Unison exactly as typed, so everything Unison accepts on the command line works the same way here. A profile named after <code>-ui graphic</code> is preselected in the profile picker.</li>
<li><strong>Settings ▸ Command Line.</strong> Shows what <code>unison</code> currently resolves to on two PATHs, the one a login shell builds and the one macOS gives commands arriving over ssh, and names it: this app's command, another copy of this app, the Homebrew <code>unison</code> formula, upstream Unison.app, a broken link, or something else. One button is offered only when the evidence supports it: Install when nothing owns the name, Repair when a broken link points at a former copy of this app, Remove for this installation's own link. Each asks for an administrator password, and each re-checks the link at the moment it runs.</li>
<li><strong>First-launch offer.</strong> When nothing on either PATH is named <code>unison</code>, or a broken link points at a former copy of this app, the app offers to install or repair the command once per launch after the profile picker appears. "Not Now" asks again next time; "Do not ask again" stops the offer, and the Settings tab remains the way to install later.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.6.1: macOS 15 or later, Apple Silicon.</li>
<li>Installing the command writes one link at <code>/usr/local/bin/unison</code>. Homebrew users get the link from the cask instead, under the Homebrew prefix, once the cask for this version is published. Only one command can hold the name: if the Homebrew <code>unison</code> formula is linked, either unlink it or keep it and install the app from the zip without the command.</li>
<li>Machines that sync to this Mac over ssh run <code>unison -server</code> here in a non-interactive shell. <code>/usr/local/bin</code> is on that shell's PATH on a stock macOS; the Homebrew prefix is not, so peers of a Homebrew install should set <code>servercmd</code> in their profile. Launch the app once from Finder before relying on it as an ssh peer, so macOS completes its first-open check of the download.</li>
<li>If automatic update checking is enabled, Sparkle notifies you when 0.7.0 is available. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.7.0/unison-ui-mac-0.7.0.app.zip" length="5260748" type="application/octet-stream" sparkle:edSignature="S9Zd+BPqWkCYkj/sjWFlZK86NWJGxXwqZdTuxAy9nln3XdLyA7ap3p5aTz5tMHAKpi9HD+kYVFFUhwLcj/joBA=="/>
        </item>
        <item>
            <title>0.6.1</title>
            <pubDate>Sun, 30 Aug 2026 01:20:37 +0000</pubDate>
            <sparkle:version>22</sparkle:version>
            <sparkle:shortVersionString>0.6.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.6.1</h1>
<p>Version 0.6.1 is a small user-interface and update-infrastructure release on top of 0.6.0. Existing profiles and settings continue to work without migration.</p>
<h2>Fixed</h2>
<ul>
<li><strong>The reconcile toolbar no longer shifts between phases.</strong> After a scan finished, the Go button used to move to where another control had been, because a single button was relabelled and resized per phase. The toolbar is now spatially stable: every button keeps its position, so Go stays where you expect it.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>Stop is a sync-only control.</strong> The Stop button keeps its "Stop" label and its position in every phase, and is active (red) only while a synchronization is running, when it aborts that sync. During a connect or scan it is inactive. To return to the profile list at any time, including during a scan, use Profiles (or ⌘⇧P). Leaving during a scan does not cancel it; the scan continues in the background. If it does not finish, quit and relaunch the app. Rescan is disabled while a scan or sync is already running.</li>
<li><strong>Update checks now go through <code>updates.courbage.net</code>.</strong> Builds from 0.6.1 onward check for updates through a project-run proxy rather than contacting GitHub Pages directly. It serves the same signed appcast byte-for-byte, so update integrity is unchanged and a signed feed is still required.</li>
</ul>
<p>When anonymous system-profile sharing is enabled, the project records a bounded per-check telemetry row containing the shared profile fields and coarse country, subject to the documented retention period. When sharing is disabled, the project creates no telemetry record, although Cloudflare still processes the request as the hosting provider. No IP address, user agent, cookie, or user identifier is stored. See <a href="https://bcourbage.github.io/unison-ui-mac/faq/#what-data-does-the-app-send">what data the app sends</a> in the FAQ.</p>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.6.0: macOS 15 or later, Apple Silicon.</li>
<li>The move to <code>updates.courbage.net</code> takes effect for update checks made by this build and later. A 0.6.0 install discovers 0.6.1 through its existing feed; once 0.6.1 is installed, its later checks use the new address.</li>
<li>If automatic update checking is enabled, Sparkle notifies you when 0.6.1 is available; whether it then downloads and installs automatically or only after you confirm depends on the separate automatic-download choice from the first-launch prompt. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.6.1/unison-ui-mac-0.6.1.app.zip" length="5173262" type="application/octet-stream" sparkle:edSignature="XmXyXDZzfnA+J2Nt8ElH8T337tZ2KXCanAMkgljh1dH68TngugVUmptUSaSOoZL+J5tDa4kx2f3g7rav3SedBw=="/>
        </item>
        <item>
            <title>0.6.0</title>
            <pubDate>Tue, 25 Aug 2026 03:14:23 +0000</pubDate>
            <sparkle:version>21</sparkle:version>
            <sparkle:shortVersionString>0.6.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.6.0</h1>
<p>Version 0.6.0 is a safety, correctness, and robustness release on top of 0.5.1. Existing profiles and settings continue to work without migration.</p>
<h2>Safety</h2>
<ul>
<li><strong>Archive maintenance is crash-safe.</strong> Clean Stale Archives, Reset Archives, and archive recovery now run as a single locked transaction: they take the same per-archive lock a live Unison uses, stage everything, and only then move the removed set to the Trash as one unit. Before the removal is committed, any failure rolls back the staged files; a crash is detected on the next launch and recovered — restoring the files if it had not yet committed, or finishing the removal if the whole set was already committed — with the locks released only after you confirm no other Unison is running.</li>
<li><strong>Clean Stale Archives only offers provably superseded copies.</strong> Orphans, "probably old" copies, anything involving a remote replica, and any archive whose roots can't be read unambiguously are now shown for reference only, never selectable and never preselected.</li>
<li><strong>The manual installer (<code>install.sh</code>) is safer.</strong> It installs atomically, so a failed install can never leave you without a working app, verifies that the download quarantine was actually cleared, and accepts only a Release build. (Homebrew-cask installs are unaffected.)</li>
<li><strong>Fixed a rare crash or corrupted results at the end of a sync</strong>, caused by values in the end-of-sync snapshot moving under the garbage collector before they were read.</li>
<li><strong>"Rescan Ignoring Archives…" can no longer overlap in-flight work.</strong> It runs only from a ready state, so it can't start on top of a connection, scan, or sync already in progress.</li>
</ul>
<h2>Fixed</h2>
<ul>
<li><strong>Editing a profile preserves a symlinked <code>.prf</code>.</strong> Saving no longer replaces a symlinked profile (for example one linked from a dotfiles repository) with a regular file, and it keeps the file's comments, ordering, and escaping intact.</li>
<li><strong>A profile the app can't read is protected.</strong> An unreadable or non-UTF-8 <code>.prf</code> is not opened as an editable empty form, so a Save can't overwrite it with a blank profile.</li>
<li><strong>Diffing a file no longer freezes the reconcile window.</strong> The window stays responsive while a diff runs; a stalled diff is reported, and other engine actions stay disabled until it returns. If it never does, quit and reopen the app.</li>
<li><strong>Ignore and Diff act on the item you mean</strong> — the selection from the menu bar, the clicked row from the right-click menu — and <strong>Select Conflicts</strong> now reveals conflicts hidden inside collapsed folders.</li>
<li><strong>No false version warning from an SSH login banner.</strong> The remote Unison-version check reads Unison's own version line, so a login banner that mentions a version number no longer triggers a spurious compatibility warning.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>Genuinely built for macOS 15.</strong> The app and its embedded Unison engine are now compiled for the macOS 15 minimum and verified on macOS 15 before release.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.5.1: macOS 15 or later, Apple Silicon.</li>
<li>If automatic update <em>checking</em> is enabled, Sparkle notifies you when 0.6.0 is available; whether it then downloads and installs automatically or only after you confirm depends on the separate automatic-download choice from the first-launch prompt. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.6.0/unison-ui-mac-0.6.0.app.zip" length="5177077" type="application/octet-stream" sparkle:edSignature="qu0rK5paQfRQqIgMKwJnkWytb/onco9sv1dvEiMVwuy6A6yJAZ5eXgpuorDb+oyiXUXQ41RVf41HUVyzb2nEDA=="/>
        </item>
    </channel>
</rss>
<!-- sparkle-signatures:
edSignature: f5jPLovZp13GdOErAFkipDkT7Uqj9BJDy2Pm2hDoptHehj8eGF5inKy0q23wCjFqxBYqzga193gjU7q+5EelAQ==
length: 12461
-->
