<?xml version="1.0" standalone="yes"?>
<!-- sparkle-sign-warning:
IMPORTANT: This file was signed by Sparkle. Any modifications to this file requires re-signing this file with generate_appcast or sign_update! The signed signature will be embedded at the end of this file.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>unison-ui-mac</title>
        <item>
            <title>0.9.0</title>
            <pubDate>Fri, 11 Sep 2026 14:24:52 +0000</pubDate>
            <sparkle:version>25</sparkle:version>
            <sparkle:shortVersionString>0.9.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.9.0</h1>
<p>Version 0.9.0 makes the app's <code>unison</code> command open the graphical app by default and act on a profile you name. When you run <code>unison &lt;profile&gt;</code> without choosing an interface, it now follows a Default interface preference that is Graphical unless you set it to Text, and a graphical launch opens that profile and starts its connection and scan right away. Existing profiles and settings continue to work without migration.</p>
<h2>Added</h2>
<ul>
<li><strong>Default interface for the <code>unison</code> command (Settings ▸ Command Line).</strong> A Graphical or Text control sets which interface <code>unison</code> uses when a command omits <code>-ui</code>. It defaults to Graphical, so typing <code>unison &lt;profile&gt;</code> opens this app. An explicit <code>-ui graphic</code> or <code>-ui text</code> always overrides the preference.</li>
<li><strong>A profile on the command line opens and scans.</strong> In graphical mode, <code>unison &lt;profile&gt;</code> opens the app and starts that profile's connection and scan right away, without a further click. It stops at the reconciliation results, so nothing is applied until you act.</li>
<li><strong>Requests reach a running instance.</strong> When the app is already running, <code>unison &lt;profile&gt;</code> is handled by that instance instead of starting a second one: it opens the profile when idle at the picker, and keeps its work and reports a clear refusal when a scan, reconciliation, sync, or a profile edit is in progress. The command returns a matching exit status.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>A bare <code>unison &lt;profile&gt;</code> now opens the app by default.</strong> Earlier versions ran Unison's text interface when <code>-ui</code> was omitted; the default is now Graphical for every account without a saved preference. If you relied on the text interface from a bare <code>unison &lt;profile&gt;</code>, set <strong>Default interface</strong> to Text in Settings ▸ Command Line, or pass <code>-ui text</code>.</li>
</ul>
<h2>Notes</h2>
<ul>
<li>Scripts and scheduled jobs should pass <code>-ui text</code> explicitly rather than depend on the default interface. With the Graphical default in effect, a headless <code>unison &lt;profile&gt;</code> (over ssh, from <code>cron</code>, or from <code>launchd</code>, with no graphical session) is refused with a message instead of running in the terminal.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.9.0/unison-ui-mac-0.9.0.app.zip" length="5613018" type="application/octet-stream" sparkle:edSignature="zSiioO2V6kIHXgeTu2heIRW1VKw3c/hjRoWfi9HmTVMN+4sfwAKAirjUGvjeeOEAtf00b8p7MyB9V+ThKQ64Cg=="/>
        </item>
        <item>
            <title>0.8.0</title>
            <pubDate>Thu, 10 Sep 2026 03:59:11 +0000</pubDate>
            <sparkle:version>24</sparkle:version>
            <sparkle:shortVersionString>0.8.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.8.0</h1>
<p>Version 0.8.0 changes how the app's <code>unison</code> command reaches your PATH and adds a guided check that a remote sync profile will use the Unison you intend on the far side. The command is now placed on your PATH by editing your own login-shell startup file, with your consent and without an administrator password; the 0.7.0 installer that wrote a link under <code>/usr/local/bin</code> is removed. Existing profiles and settings continue to work without migration.</p>
<h2>Added</h2>
<ul>
<li><strong>Guided remote check.</strong> Before you depend on an ssh profile, get a read on what the far side will run: a version or engine mismatch on the remote is a common cause of sync failures, and it is easier to look up front than mid-sync. The check tests whether this profile's remote command reports a Unison version over ssh, and offers discovered alternatives if you want to change it. Discovery uses a plain <code>sh</code> lookup, which may resolve commands differently from the profile. Only a synchronization confirms the server protocol. It changes nothing on either side, is reachable from the profile picker and the Profile Editor, states the current result first, titles its alternatives by their consequence, and reports a failed connection plainly. Effective-value fields show the actual host, user, port, and remote command a profile will use; the advice that avoids the question is an absolute <code>servercmd</code> in the peer's profile, which the pane can copy and the check can assess.</li>
<li><strong>Command-line setup through your login shell (Settings ▸ Command Line).</strong> So you can type <code>unison</code> and get this app — <code>unison -ui graphic</code> opens it, and the same install serves Unison's text interface and its ssh-server role — the app can put its command on your PATH. Its command lives inside the bundle at <code>Contents/SharedSupport/bin/unison</code>. Add Terminal Setup puts that directory on your PATH by writing a marked, app-managed block to your own shell startup file — automatically only when the case is unambiguous and safe (a recognized stock zsh startup, this app's own earlier block, or a stated set of bash and fish cases), and otherwise as Manual setup, where the pane shows what to add and names the file to edit when it can identify one — when it cannot safely identify the file, such as a redirected <code>ZDOTDIR</code> or an unsupported shell, it says so rather than point at the wrong file. The pane leads with what <code>unison</code> resolves to in your login shell and names it — this app, another copy, no unison, could not be checked, or manual — with the resolved path shown as evidence, and offers Use This Copy, Remove Terminal Setup, and copying the app's command path. Removing restores the file with its other contents intact. No administrator password, and nothing is written outside your own files. macOS 15 and macOS 26 stock <code>/etc/zprofile</code> are recognized so a fresh account is offered automatic zsh setup; an unrecognized system file falls back to Manual setup rather than guessing.</li>
<li><strong>Profile Editor keyboard navigation.</strong> The General, Roots, File Attributes, Options, and Includes sections of the Profile Editor are navigable from the keyboard.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>Homebrew cask is <code>unison-ui</code>.</strong> Homebrew's token rules drop platform suffixes, so the tap's cask is <code>bcourbage/tap/unison-ui</code>; installs under the old token migrate on their own at the next <code>brew update</code>, and Settings ▸ Command Line recognizes a Homebrew-managed install under either token.</li>
</ul>
<h2>Removed</h2>
<ul>
<li><strong>The 0.7.0 privileged command installer</strong> and its administrator prompt. The <code>unison</code> command is now placed on your PATH through your own login-shell startup file (Settings ▸ Command Line). A <code>/usr/local/bin/unison</code> link left by 0.7.0 keeps working — it points at the app's launcher, and Settings reports it as this app — and is not removed automatically; remove it yourself if you no longer want it.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.7.0: macOS 15 or later, Apple Silicon.</li>
<li>Automatic updates are delivered through Sparkle; Homebrew users update with <code>brew upgrade --cask unison-ui</code>.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.8.0/unison-ui-mac-0.8.0.app.zip" length="5574875" type="application/octet-stream" sparkle:edSignature="7Qri2gl5/XeO78sjFd9gfnbqoLlcKy6PiijNncrSuR9EBrDZBqY2xULqGWN2U3b/oLP4+axOxpdP3hgAa6gsBA=="/>
        </item>
        <item>
            <title>0.7.0</title>
            <pubDate>Sun, 06 Sep 2026 09:40:21 +0000</pubDate>
            <sparkle:version>23</sparkle:version>
            <sparkle:shortVersionString>0.7.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.7.0</h1>
<p>Version 0.7.0 adds a <code>unison</code> command to the app. One install now serves both the graphical interface and Unison's command-line roles, including acting as the far side of an SSH profile, so a Mac with this app no longer needs a separate Unison installation. Existing profiles and settings continue to work without migration.</p>
<h2>Added</h2>
<ul>
<li><strong>The <code>unison</code> command.</strong> The app bundle carries a command-line launcher. Once linked onto your PATH under the name <code>unison</code>, it behaves like Unison's own command: <code>unison -ui graphic</code> opens this app, <code>unison &lt;profile&gt;</code> and <code>unison -ui text &lt;profile&gt;</code> run Unison's text interface in the terminal on the app's embedded engine, and <code>unison -server</code>, which another machine runs over ssh when it syncs to this Mac, is served by the app's engine headlessly. Arguments reach Unison exactly as typed, so everything Unison accepts on the command line works the same way here. A profile named after <code>-ui graphic</code> is preselected in the profile picker.</li>
<li><strong>Settings ▸ Command Line.</strong> Shows what <code>unison</code> currently resolves to on two PATHs, the one a login shell builds and the one macOS gives commands arriving over ssh, and names it: this app's command, another copy of this app, the Homebrew <code>unison</code> formula, upstream Unison.app, a broken link, or something else. One button is offered only when the evidence supports it: Install when nothing owns the name, Repair when a broken link points at a former copy of this app, Remove for this installation's own link. Each asks for an administrator password, and each re-checks the link at the moment it runs.</li>
<li><strong>First-launch offer.</strong> When nothing on either PATH is named <code>unison</code>, or a broken link points at a former copy of this app, the app offers to install or repair the command once per launch after the profile picker appears. "Not Now" asks again next time; "Do not ask again" stops the offer, and the Settings tab remains the way to install later.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.6.1: macOS 15 or later, Apple Silicon.</li>
<li>Installing the command writes one link at <code>/usr/local/bin/unison</code>. Homebrew users get the link from the cask instead, under the Homebrew prefix, once the cask for this version is published. Only one command can hold the name: if the Homebrew <code>unison</code> formula is linked, either unlink it or keep it and install the app from the zip without the command.</li>
<li>Machines that sync to this Mac over ssh run <code>unison -server</code> here in a non-interactive shell. <code>/usr/local/bin</code> is on that shell's PATH on a stock macOS; the Homebrew prefix is not, so peers of a Homebrew install should set <code>servercmd</code> in their profile. Launch the app once from Finder before relying on it as an ssh peer, so macOS completes its first-open check of the download.</li>
<li>If automatic update checking is enabled, Sparkle notifies you when 0.7.0 is available. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.7.0/unison-ui-mac-0.7.0.app.zip" length="5260748" type="application/octet-stream" sparkle:edSignature="S9Zd+BPqWkCYkj/sjWFlZK86NWJGxXwqZdTuxAy9nln3XdLyA7ap3p5aTz5tMHAKpi9HD+kYVFFUhwLcj/joBA=="/>
        </item>
    </channel>
</rss>
<!-- sparkle-signatures:
edSignature: 5wodQKp4U/8AEe1wRI2m755RFvTLEQSQCRfb2/z7MZMChNK1hDj1Rree9YsC6B/tqo4TLxGtmC8y8DQ3wcvtAg==
length: 12961
-->
