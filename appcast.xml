<?xml version="1.0" standalone="yes"?>
<!-- sparkle-sign-warning:
IMPORTANT: This file was signed by Sparkle. Any modifications to this file requires re-signing this file with generate_appcast or sign_update! The signed signature will be embedded at the end of this file.
-->
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>unison-ui-mac</title>
        <item>
            <title>0.6.0</title>
            <pubDate>Tue, 25 Aug 2026 03:14:23 +0000</pubDate>
            <sparkle:version>21</sparkle:version>
            <sparkle:shortVersionString>0.6.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison UI for macOS 0.6.0</h1>
<p>Version 0.6.0 is a safety, correctness, and robustness release on top of 0.5.1. Existing profiles and settings continue to work without migration.</p>
<h2>Safety</h2>
<ul>
<li><strong>Archive maintenance is crash-safe.</strong> Clean Stale Archives, Reset Archives, and archive recovery now run as a single locked transaction: they take the same per-archive lock a live Unison uses, stage everything, and only then move the removed set to the Trash as one unit. Before the removal is committed, any failure rolls back the staged files; a crash is detected on the next launch and recovered — restoring the files if it had not yet committed, or finishing the removal if the whole set was already committed — with the locks released only after you confirm no other Unison is running.</li>
<li><strong>Clean Stale Archives only offers provably superseded copies.</strong> Orphans, "probably old" copies, anything involving a remote replica, and any archive whose roots can't be read unambiguously are now shown for reference only, never selectable and never preselected.</li>
<li><strong>The manual installer (<code>install.sh</code>) is safer.</strong> It installs atomically, so a failed install can never leave you without a working app, verifies that the download quarantine was actually cleared, and accepts only a Release build. (Homebrew-cask installs are unaffected.)</li>
<li><strong>Fixed a rare crash or corrupted results at the end of a sync</strong>, caused by values in the end-of-sync snapshot moving under the garbage collector before they were read.</li>
<li><strong>"Rescan Ignoring Archives…" can no longer overlap in-flight work.</strong> It runs only from a ready state, so it can't start on top of a connection, scan, or sync already in progress.</li>
</ul>
<h2>Fixed</h2>
<ul>
<li><strong>Editing a profile preserves a symlinked <code>.prf</code>.</strong> Saving no longer replaces a symlinked profile (for example one linked from a dotfiles repository) with a regular file, and it keeps the file's comments, ordering, and escaping intact.</li>
<li><strong>A profile the app can't read is protected.</strong> An unreadable or non-UTF-8 <code>.prf</code> is not opened as an editable empty form, so a Save can't overwrite it with a blank profile.</li>
<li><strong>Diffing a file no longer freezes the reconcile window.</strong> The window stays responsive while a diff runs; a stalled diff is reported, and other engine actions stay disabled until it returns. If it never does, quit and reopen the app.</li>
<li><strong>Ignore and Diff act on the item you mean</strong> — the selection from the menu bar, the clicked row from the right-click menu — and <strong>Select Conflicts</strong> now reveals conflicts hidden inside collapsed folders.</li>
<li><strong>No false version warning from an SSH login banner.</strong> The remote Unison-version check reads Unison's own version line, so a login banner that mentions a version number no longer triggers a spurious compatibility warning.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>Genuinely built for macOS 15.</strong> The app and its embedded Unison engine are now compiled for the macOS 15 minimum and verified on macOS 15 before release.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.5.1: macOS 15 or later, Apple Silicon.</li>
<li>If automatic update <em>checking</em> is enabled, Sparkle notifies you when 0.6.0 is available; whether it then downloads and installs automatically or only after you confirm depends on the separate automatic-download choice from the first-launch prompt. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.6.0/unison-ui-mac-0.6.0.app.zip" length="5177077" type="application/octet-stream" sparkle:edSignature="qu0rK5paQfRQqIgMKwJnkWytb/onco9sv1dvEiMVwuy6A6yJAZ5eXgpuorDb+oyiXUXQ41RVf41HUVyzb2nEDA=="/>
        </item>
        <item>
            <title>0.5.1</title>
            <pubDate>Sun, 23 Aug 2026 01:44:39 +0000</pubDate>
            <sparkle:version>20</sparkle:version>
            <sparkle:shortVersionString>0.5.1</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison-UI-Mac 0.5.1</h1>
<p>Version 0.5.1 is a safety and documentation release on top of 0.5.0. Its main change is how the app behaves when you leave a profile while it is still scanning. Existing profiles and settings continue to work without migration.</p>
<h2>Changed</h2>
<ul>
<li><strong>Leaving during a scan returns to the profile list without cancelling the scan.</strong> While a scan is running, the toolbar's Stop control is <strong>Return to Profiles</strong>: it takes you back to the profile list but does not stop the scan itself, which finishes in the background. There is no in-place "stop the scan" action. If a remote scan hangs, quit and relaunch the app.</li>
</ul>
<h2>Fixed</h2>
<ul>
<li><strong>File-integrity safeguard for interrupted scans.</strong> Earlier builds could, in some cases, interrupt a running scan and then keep using the same synchronization engine. Because that engine's internal state could not be verified after a forced interruption, the app no longer does this — protecting your files and Unison's archives is the priority. Ordinary <strong>Sync Stop</strong> (during an actual file transfer) is unchanged and fully supported.</li>
<li>Corrected the built-in <strong>Manual</strong> and the README so they accurately describe the current behavior — the Settings tabs and menus, the SSH version check and compatibility boundary, and how the app updates itself.</li>
</ul>
<h2>Security</h2>
<ul>
<li><strong>Credential prompts default to masked entry.</strong> When SSH asks for a password, key passphrase, or verification code, the app now always shows a masked (secure) field; only the exact host-key "are you sure you want to continue connecting (yes/no)?" question uses a plain field. A prompt that merely mentions "authenticity" or <code>yes/no</code>, or a host-key line immediately followed by a password prompt, is treated as a secret and masked — where an earlier build could have shown it in a plain, readable field.</li>
</ul>
<h2>If you are still on 0.5.0</h2>
<p>Until you have updated to 0.5.1: if a remote scan hangs, <strong>quit and relaunch</strong> rather than using Stop Scan followed by Rescan.</p>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.5.0 (macOS 15 or later, Apple Silicon).</li>
<li>If automatic update *checking* is enabled, Sparkle notifies you when 0.5.1 is available; whether the download and install then happen automatically or only after you confirm depends on the separate automatic-download choice from the first-launch prompt. You can also update any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.5.1/unison-ui-mac-0.5.1.app.zip" length="5072252" type="application/octet-stream" sparkle:edSignature="ocTijlx9j/Gj0LHjVdp8HIn+pME48fTwV4iiIom0PZ+HId23Lp73uIBctcVye1SE2mkO27IKhzHZTYLZdKuDDw=="/>
        </item>
        <item>
            <title>0.5.0</title>
            <pubDate>Fri, 21 Aug 2026 23:57:05 +0000</pubDate>
            <sparkle:version>19</sparkle:version>
            <sparkle:shortVersionString>0.5.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h1>Unison-UI-Mac 0.5.0</h1>
<p>Version 0.5.0 adds in-app updates, so the app can find and install new versions on its own, and moves distribution to a Developer ID-signed, notarized build. It also adds a Software Updates settings tab and a Donate menu item. It builds on 0.4.2.</p>
<p>Existing profiles and settings continue to work without migration.</p>
<h2>Added</h2>
<ul>
<li><strong>In-app updates.</strong> The app can now check for updates and install them itself, through the Sparkle update framework. Use App menu ▸ Check for Updates at any time. On first launch the app asks whether to check automatically.</li>
<li><strong>Software Updates settings.</strong> A new Updates tab in Settings lets you change these choices at any time: whether the app checks for updates automatically, and whether it includes an anonymous system profile when it checks.</li>
<li><strong>Donate menu item.</strong> Help ▸ Donate opens the project's GitHub Sponsors page.</li>
</ul>
<h2>Changed</h2>
<ul>
<li><strong>Signed and notarized distribution.</strong> The app is now distributed as a Developer ID-signed, notarized build, so it opens with a normal double-click. Updates after the first install come through the app itself.</li>
</ul>
<h2>Compatibility</h2>
<ul>
<li>No profile or settings migration is required. Same system requirements as 0.4.2 (macOS 15 or later, Apple Silicon).</li>
<li>If you installed an earlier version by hand, install 0.5.0 once (via Homebrew or the download); after that the app updates itself, automatically if you enable automatic checks or any time from App menu ▸ Check for Updates.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/bcourbage/unison-ui-mac/releases/download/v0.5.0/unison-ui-mac-0.5.0.app.zip" length="5124923" type="application/octet-stream" sparkle:edSignature="jX7828uMsaymqtzWuC4aPLsxHf9bcyHFj/DOrfv4tpNCkxC3D+Oid2K4cCeaENyQYLYMoa89ayHymOs57kjyAA=="/>
        </item>
    </channel>
</rss>
<!-- sparkle-signatures:
edSignature: yWeHHTD7E6HM4SImda2mrRSKAjdNUfK1BviEJb6kKUHm17YAmsnc3DcXOxKvAIhT+fSsWlVqKve+T42ZhB71CA==
length: 10732
-->
